node_modules/
package-lock.json

# Working extraction area — OCR text + page renders of the source PDF. Local
# planning material only; never committed.
_work/

# LevelDB compendium packs are a BUILD ARTIFACT regenerated from packs-src/ by
# `npm run build-packs`. Not tracked (Foundry compacts them every session); the
# release workflow rebuilds them from packs-src before packaging.
/packs/

# Re-include assets/icons (the global ~/.gitignore_global "Icon?" rule matches
# the "icons" dir name case-insensitively on macOS).
!assets/icons/
!assets/icons/*

.DS_Store
